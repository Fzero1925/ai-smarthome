--- /mnt/data/generate_image_sitemap.py+++ /mnt/data/generate_image_sitemap.py (modified)@@ -169,4 +169,10 @@         print(f"🎉 Success: Created {output_file} with {count} images")
     except Exception as e:
         print(f"❌ Error generating image sitemap: {e}")
-        sys.exit(1)+        sys.exit(1)
+
+# === v2: fix base_url and filter low quality images ===
+BASE_URL = "https://www.ai-smarthomehub.com"
+def normalize_base_url(u: str):
+    if u.startswith('http'): return u
+    return BASE_URL.rstrip('/') + '/' + u.lstrip('/')
