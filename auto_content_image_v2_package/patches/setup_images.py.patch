--- /mnt/data/setup_images.py+++ /mnt/data/setup_images.py (modified)@@ -143,4 +143,9 @@ 
 
 if __name__ == "__main__":
-    main()+    main()
+
+# === v2: enforce env policy for placeholders ===
+import os
+ENV = os.getenv('ENV','dev')
+ALLOW_PLACEHOLDER = (ENV != 'prod')
